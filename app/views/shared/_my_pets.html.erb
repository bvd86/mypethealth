<div class="my-2 d-flex justify-content-between">
  <h2>My pets</h2>
  <%= link_to "➕ Add a pet", new_user_pet_path(@user) %>
</div>

<% @user.pets.each do |p| %>
<p>Name: <%= p.name %></p>
<p>Species: <%= p.species %></p>
<p>Breed: <%= p.breed %></p>
<%= cl_image_tag p.photo.key, alt: p.species, height: 200, width: 200 %>
<%= link_to "Edit", edit_user_pet_path(@user, p.id) %>
<p>----------------------------</p>
<% end %>
